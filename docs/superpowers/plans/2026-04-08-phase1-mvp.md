# Phase 1: MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** キノコ図鑑の閲覧・検索・シーズンカレンダーとPWAオフライン基盤を構築する。

**Architecture:** Next.js 15 App Router で Static Export した PWA。図鑑データは mushrooms.json としてバンドルし、全ページを 'use client' でクライアントサイドレンダリング。Vitest + React Testing Library による TDD ワークフロー。

**Tech Stack:** Next.js 15, TypeScript (strict), Tailwind CSS, @ducanh2912/next-pwa, Vitest, React Testing Library, Playwright

**Design doc:** `docs/superpowers/specs/2026-04-08-myconote-design.md`

---

## File Structure

Phase 1 で作成するファイル一覧:

```
src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── globals.css
│   ├── zukan/
│   │   ├── page.tsx
│   │   └── [id]/page.tsx
│   ├── identify/page.tsx          # stub
│   ├── plan/page.tsx              # stub
│   ├── records/page.tsx           # stub
│   └── settings/page.tsx
├── components/
│   ├── layout/
│   │   ├── BottomNav.tsx
│   │   ├── BottomNav.test.tsx
│   │   ├── OfflineBanner.tsx
│   │   ├── OfflineBanner.test.tsx
│   │   └── PageHeader.tsx
│   ├── zukan/
│   │   ├── MushroomCard.tsx
│   │   ├── MushroomCard.test.tsx
│   │   ├── MushroomDetail.tsx
│   │   ├── MushroomDetail.test.tsx
│   │   ├── ToxicityBadge.tsx
│   │   ├── ToxicityBadge.test.tsx
│   │   ├── SeasonBar.tsx
│   │   ├── SeasonBar.test.tsx
│   │   ├── SeasonCalendar.tsx
│   │   ├── SeasonCalendar.test.tsx
│   │   ├── SearchFilter.tsx
│   │   └── SearchFilter.test.tsx
│   └── ui/
│       ├── Button.tsx
│       └── LoadingSpinner.tsx
├── contexts/
│   ├── AppContext.tsx
│   └── AppContext.test.tsx
├── constants/
│   ├── toxicity.ts
│   ├── toxicity.test.ts
│   ├── theme.ts
│   └── ui-text.ts
├── types/
│   ├── mushroom.ts
│   ├── record.ts
│   └── settings.ts
├── data/
│   ├── mushrooms.json
│   ├── mushrooms.ts
│   └── mushrooms.test.ts
├── test-setup.ts
public/
├── images/mushrooms/placeholder.svg
├── manifest.json
└── icons/
    ├── icon-192x192.png
    └── icon-512x512.png
vitest.config.ts
```

---

## Task 1: Project Scaffolding & Configuration

**Files:**
- Create: `package.json`, `next.config.ts`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.mjs`, `vitest.config.ts`, `src/test-setup.ts`, `src/app/globals.css`, `.gitignore` (update)

- [ ] **Step 1: Initialize Next.js project**

```bash
npx create-next-app@latest . --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
```

もし既存ファイルとの競合でエラーが出る場合は、一時ディレクトリで生成してからファイルを移動する:
```bash
cd /tmp && npx create-next-app@latest myconote-temp --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
cp -r /tmp/myconote-temp/{package.json,next.config.ts,tsconfig.json,tailwind.config.ts,postcss.config.mjs,src,public,.eslintrc.json} /c/Users/asaku/Desktop/pc_data/works/MycoNote/
rm -rf /tmp/myconote-temp
```

- [ ] **Step 2: Install additional dependencies**

```bash
npm install dexie @ducanh2912/next-pwa
```

- [ ] **Step 3: Install dev dependencies**

```bash
npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event @types/react @types/react-dom playwright @playwright/test
```

- [ ] **Step 4: Configure Vitest**

`vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test-setup.ts'],
    css: true,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

`src/test-setup.ts`:
```typescript
import '@testing-library/jest-dom/vitest';
```

- [ ] **Step 5: Add test scripts to package.json**

`package.json` の `"scripts"` に追加:
```json
{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  }
}
```

- [ ] **Step 6: Configure Next.js for Static Export + PWA**

`next.config.ts`:
```typescript
import type { NextConfig } from 'next';
import withPWAInit from '@ducanh2912/next-pwa';

const withPWA = withPWAInit({
  dest: 'public',
  disable: process.env.NODE_ENV === 'development',
  register: true,
  skipWaiting: true,
});

const nextConfig: NextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
};

export default withPWA(nextConfig);
```

- [ ] **Step 7: Configure Tailwind with forest theme**

`tailwind.config.ts`:
```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        forest: {
          50: '#f0f7ec',
          100: '#d8ebd0',
          200: '#b4d9a4',
          300: '#8bc34a',
          400: '#6fa030',
          500: '#4a7c23',
          600: '#3d6620',
          700: '#2d5016',
          800: '#1e3a0e',
          900: '#142808',
          950: '#0a1504',
        },
      },
    },
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 8: Set up globals.css**

`src/app/globals.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-forest-950 text-forest-50;
  }
}
```

- [ ] **Step 9: Create directory structure**

```bash
mkdir -p src/{components/{layout,zukan,identify,records,plan,ui},contexts,constants,types,data}
mkdir -p public/{images/mushrooms,icons}
```

- [ ] **Step 10: Create placeholder mushroom image**

`public/images/mushrooms/placeholder.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" fill="none">
  <rect width="200" height="200" rx="8" fill="#1e3a0e"/>
  <text x="100" y="90" text-anchor="middle" font-size="64">🍄</text>
  <text x="100" y="130" text-anchor="middle" font-size="14" fill="#8bc34a">No Image</text>
</svg>
```

- [ ] **Step 11: Update .gitignore**

`.gitignore` に以下を追加（create-next-app が生成したものとマージ）:
```gitignore
# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
coverage/

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem
Thumbs.db

# Debug
npm-debug.log*

# Local env files
.env*.local

# PWA
public/sw.js
public/workbox-*.js
public/swe-worker-*.js

# IDE
.vscode/
.idea/

# Superpowers
.superpowers/
```

- [ ] **Step 12: Verify dev server starts**

```bash
npm run dev
```

Expected: Next.js dev server starts at http://localhost:3000 without errors.

- [ ] **Step 13: Verify test runner works**

一時的なテストファイルを作成して実行:
```bash
echo 'import { describe, it, expect } from "vitest"; describe("setup", () => { it("works", () => { expect(true).toBe(true); }); });' > src/smoke.test.ts
npm test
rm src/smoke.test.ts
```

Expected: 1 test passed.

- [ ] **Step 14: Commit**

```bash
git add -A
git commit -m "chore: scaffold Next.js 15 project with Tailwind, Vitest, PWA config"
```

---

## Task 2: Type Definitions & Constants

**Files:**
- Create: `src/types/mushroom.ts`, `src/types/record.ts`, `src/types/settings.ts`, `src/constants/toxicity.ts`, `src/constants/toxicity.test.ts`, `src/constants/theme.ts`, `src/constants/ui-text.ts`

- [ ] **Step 1: Create Mushroom type definitions**

`src/types/mushroom.ts`:
```typescript
export type Toxicity = 'edible' | 'edible_caution' | 'inedible' | 'toxic' | 'deadly_toxic';

export interface Mushroom {
  id: string;
  names: {
    ja: string;
    scientific: string;
    aliases?: string[];
  };
  toxicity: Toxicity;
  season: {
    start_month: number;
    end_month: number;
  };
  habitat: string[];
  regions: string[];
  image_local: string;
  images_remote: string[];
  description: string;
  features: string;
  similar_species: string[];
  caution?: string;
  tree_association?: string[];
  source_url?: string;
}

export interface FilterOptions {
  query?: string;
  toxicity?: Toxicity[];
  season?: number;
  habitat?: string;
  hasRecord?: boolean;
}
```

- [ ] **Step 2: Create Record type definitions**

`src/types/record.ts`:
```typescript
export interface MushroomRecord {
  id: string;
  mushroom_id?: string;
  mushroom_name_ja?: string;
  observed_at: string;
  location: {
    lat: number;
    lng: number;
    description?: string;
  };
  photos: string[];
  quantity?: string;
  memo?: string;
  harvested: boolean;
  tags?: string[];
  created_at: string;
  updated_at: string;
}
```

- [ ] **Step 3: Create Settings type definitions**

`src/types/settings.ts`:
```typescript
export interface AppSettings {
  anthropic_api_key?: string;
  preferred_regions?: string[];
  theme?: 'light' | 'dark' | 'system';
}
```

- [ ] **Step 4: Write failing test for toxicity constants**

`src/constants/toxicity.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { TOXICITY_CONFIG, getToxicityConfig } from './toxicity';
import type { Toxicity } from '@/types/mushroom';

describe('TOXICITY_CONFIG', () => {
  it('defines config for all 5 toxicity levels', () => {
    const levels: Toxicity[] = ['edible', 'edible_caution', 'inedible', 'toxic', 'deadly_toxic'];
    for (const level of levels) {
      expect(TOXICITY_CONFIG[level]).toBeDefined();
      expect(TOXICITY_CONFIG[level].label).toBeTruthy();
      expect(TOXICITY_CONFIG[level].color).toBeTruthy();
      expect(TOXICITY_CONFIG[level].icon).toBeTruthy();
    }
  });

  it('assigns higher priority to more dangerous toxicity levels', () => {
    expect(TOXICITY_CONFIG.edible.priority).toBeLessThan(TOXICITY_CONFIG.toxic.priority);
    expect(TOXICITY_CONFIG.toxic.priority).toBeLessThan(TOXICITY_CONFIG.deadly_toxic.priority);
  });
});

describe('getToxicityConfig', () => {
  it('returns correct config for a given toxicity', () => {
    const config = getToxicityConfig('deadly_toxic');
    expect(config.label).toBe('猛毒');
    expect(config.color).toContain('red');
  });
});
```

- [ ] **Step 5: Run test to verify it fails**

```bash
npm test -- src/constants/toxicity.test.ts
```

Expected: FAIL — module './toxicity' not found.

- [ ] **Step 6: Implement toxicity constants**

`src/constants/toxicity.ts`:
```typescript
import type { Toxicity } from '@/types/mushroom';

export interface ToxicityInfo {
  label: string;
  color: string;
  textColor: string;
  icon: string;
  priority: number;
}

export const TOXICITY_CONFIG: Record<Toxicity, ToxicityInfo> = {
  edible: {
    label: '食用',
    color: 'bg-green-600',
    textColor: 'text-green-600',
    icon: '✓',
    priority: 0,
  },
  edible_caution: {
    label: '要注意',
    color: 'bg-yellow-600',
    textColor: 'text-yellow-600',
    icon: '⚠',
    priority: 1,
  },
  inedible: {
    label: '不食',
    color: 'bg-gray-500',
    textColor: 'text-gray-500',
    icon: '—',
    priority: 2,
  },
  toxic: {
    label: '毒',
    color: 'bg-orange-600',
    textColor: 'text-orange-600',
    icon: '⚠',
    priority: 3,
  },
  deadly_toxic: {
    label: '猛毒',
    color: 'bg-red-600',
    textColor: 'text-red-600',
    icon: '☠',
    priority: 4,
  },
} as const;

export function getToxicityConfig(toxicity: Toxicity): ToxicityInfo {
  return TOXICITY_CONFIG[toxicity];
}
```

- [ ] **Step 7: Run test to verify it passes**

```bash
npm test -- src/constants/toxicity.test.ts
```

Expected: 3 tests passed.

- [ ] **Step 8: Create theme constants**

`src/constants/theme.ts`:
```typescript
export const THEME = {
  colors: {
    background: {
      primary: 'bg-forest-950',
      secondary: 'bg-forest-900',
      card: 'bg-forest-800',
      cardHover: 'hover:bg-forest-700',
    },
    text: {
      primary: 'text-forest-50',
      secondary: 'text-forest-200',
      muted: 'text-forest-400',
    },
    accent: {
      primary: 'text-forest-300',
      bg: 'bg-forest-300',
    },
    border: {
      default: 'border-forest-700',
      light: 'border-forest-600',
    },
    nav: {
      bg: 'bg-forest-900',
      active: 'text-forest-300',
      inactive: 'text-forest-500',
    },
  },
} as const;
```

- [ ] **Step 9: Create UI text constants**

`src/constants/ui-text.ts`:
```typescript
export const UI_TEXT = {
  nav: {
    zukan: '図鑑',
    identify: '識別',
    plan: '計画',
    records: '記録',
    settings: '設定',
  },
  zukan: {
    title: 'キノコ図鑑',
    searchPlaceholder: '名前で検索...',
    filterToxicity: '毒性',
    filterSeason: '季節',
    filterHabitat: '生育地',
    noResults: '該当するキノコが見つかりません',
    season: 'シーズン',
    habitat: '生育地',
    regions: '分布地域',
    features: '形態的特徴',
    description: '概要',
    similarSpecies: '似ている種',
    caution: '注意事項',
    treeAssociation: '関連する樹種',
    seasonCalendarTitle: 'シーズンカレンダー',
    myRecords: '自分の採取記録',
    noRecords: 'この種の採取記録はありません',
  },
  identify: {
    title: '識別',
    comingSoon: 'この機能はPhase 3以降で実装予定です',
  },
  plan: {
    title: '採取計画',
    comingSoon: 'この機能はPhase 3以降で実装予定です',
  },
  records: {
    title: '採取記録',
    comingSoon: 'この機能はPhase 2以降で実装予定です',
  },
  settings: {
    title: '設定',
    appInfo: 'アプリ情報',
    appName: 'MycoNote',
    appDescription: 'キノコ採取・観察ハンドブック',
    version: 'v0.1.0',
  },
  common: {
    loading: '読み込み中...',
    error: 'エラーが発生しました',
    offline: 'オフラインモード — 一部機能が制限されています',
    onlineRestored: 'オンラインに復帰しました',
    back: '戻る',
  },
  months: ['1月', '2月', '3月', '4月', '5月', '6月', '7月', '8月', '9月', '10月', '11月', '12月'],
} as const;
```

- [ ] **Step 10: Commit**

```bash
git add src/types/ src/constants/
git commit -m "feat: add type definitions and constants (toxicity, theme, UI text)"
```

---

## Task 3: Sample Data & Data Module

**Files:**
- Create: `src/data/mushrooms.json`, `src/data/mushrooms.ts`, `src/data/mushrooms.test.ts`

- [ ] **Step 1: Create sample mushroom data**

`src/data/mushrooms.json`:
```json
[
  {
    "id": "matsutake",
    "names": { "ja": "マツタケ", "scientific": "Tricholoma matsutake", "aliases": ["松茸"] },
    "toxicity": "edible",
    "season": { "start_month": 9, "end_month": 11 },
    "habitat": ["アカマツ林", "針葉樹林"],
    "regions": ["北海道", "本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "日本を代表する高級食用キノコ。アカマツの根に菌根を形成して生育する。独特の強い芳香が特徴で、秋の味覚として珍重される。",
    "features": "傘は褐色で繊維状の鱗片がある。柄は太く充実し、つばがある。肉は白く締まっており、特有の香りがある。",
    "similar_species": [],
    "tree_association": ["アカマツ", "コメツガ", "ツガ"],
    "source_url": "https://ja.wikipedia.org/wiki/マツタケ"
  },
  {
    "id": "shiitake",
    "names": { "ja": "シイタケ", "scientific": "Lentinula edodes", "aliases": ["椎茸"] },
    "toxicity": "edible",
    "season": { "start_month": 3, "end_month": 5 },
    "habitat": ["広葉樹林", "シイ・クヌギの枯木"],
    "regions": ["北海道", "本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "日本で最も広く栽培・消費されるキノコ。野生では広葉樹の枯木や倒木に発生する。旨味成分が豊富で、乾燥させるとさらに風味が増す。",
    "features": "傘は茶褐色で、表面に白いひび割れ模様が入ることがある。ひだは白色。柄は硬く繊維質。",
    "similar_species": ["tsukiyo-take"],
    "caution": "ツキヨタケと混同しないよう注意。シイタケは柄が中心につき、ひだは白色。",
    "tree_association": ["シイ", "クヌギ", "コナラ"],
    "source_url": "https://ja.wikipedia.org/wiki/シイタケ"
  },
  {
    "id": "enokitake",
    "names": { "ja": "エノキタケ", "scientific": "Flammulina velutipes", "aliases": ["榎茸", "ナメタケ"] },
    "toxicity": "edible",
    "season": { "start_month": 11, "end_month": 3 },
    "habitat": ["広葉樹の枯木", "エノキ・ケヤキの切株"],
    "regions": ["北海道", "本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "冬季に発生する食用キノコ。栽培品はもやし状の白い姿だが、野生では茶色い傘と短い柄を持つ全く異なる外見。",
    "features": "野生品は傘が茶褐色で粘性がある。柄の下部はビロード状の暗褐色。栽培品とは大きく外見が異なる。",
    "similar_species": ["nigakuri-take"],
    "caution": "野生品を採取する場合、ニガクリタケとの区別に注意。",
    "tree_association": ["エノキ", "ケヤキ", "コナラ"],
    "source_url": "https://ja.wikipedia.org/wiki/エノキタケ"
  },
  {
    "id": "nameko",
    "names": { "ja": "ナメコ", "scientific": "Pholiota microspora" },
    "toxicity": "edible",
    "season": { "start_month": 9, "end_month": 11 },
    "habitat": ["ブナ林", "広葉樹の枯木"],
    "regions": ["北海道", "本州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "表面のぬめりが特徴的な食用キノコ。味噌汁の具として広く親しまれている。ブナの枯木に群生する。",
    "features": "傘は赤褐色〜茶褐色で、表面に厚いゼラチン質のぬめりがある。ひだはやや密で淡褐色。",
    "similar_species": ["nigakuri-take"],
    "caution": "ニガクリタケとの誤食に注意。ナメコはぬめりがあり苦味がない。",
    "tree_association": ["ブナ", "ナラ"],
    "source_url": "https://ja.wikipedia.org/wiki/ナメコ"
  },
  {
    "id": "hiratake",
    "names": { "ja": "ヒラタケ", "scientific": "Pleurotus ostreatus", "aliases": ["カンタケ"] },
    "toxicity": "edible",
    "season": { "start_month": 10, "end_month": 3 },
    "habitat": ["広葉樹の枯木", "倒木"],
    "regions": ["北海道", "本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "世界中で食用とされるキノコ。広葉樹の枯木に重なり合って発生する。栽培も盛んに行われている。",
    "features": "傘は灰色〜暗褐色で扇形。ひだは白色で柄に長く垂生する。柄は短く偏心する。",
    "similar_species": ["tsukiyo-take"],
    "caution": "ツキヨタケとの誤食事故が多い。ツキヨタケは柄の断面に黒いシミがある。",
    "tree_association": ["ブナ", "ミズナラ", "カエデ"],
    "source_url": "https://ja.wikipedia.org/wiki/ヒラタケ"
  },
  {
    "id": "tamago-tengu-take",
    "names": { "ja": "タマゴテングタケ", "scientific": "Amanita phalloides" },
    "toxicity": "deadly_toxic",
    "season": { "start_month": 6, "end_month": 10 },
    "habitat": ["広葉樹林", "コナラ・シイ林"],
    "regions": ["本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "世界で最も多くの死亡事故を起こしている猛毒キノコ。アマトキシン類を含み、摂取後6〜24時間で激しい消化器症状を引き起こし、肝臓・腎臓を破壊する。",
    "features": "傘はオリーブ色〜黄緑色。柄は白色で基部にふくらみ（つぼ）がある。つばがある。ひだは白色。",
    "similar_species": [],
    "caution": "致死量はわずか1本分。解毒剤は存在しない。類似の食用キノコはないが、幼菌は他種と混同される危険がある。絶対に採取・食用しないこと。",
    "tree_association": ["コナラ", "シイ"],
    "source_url": "https://ja.wikipedia.org/wiki/タマゴテングタケ"
  },
  {
    "id": "doku-tsuru-take",
    "names": { "ja": "ドクツルタケ", "scientific": "Amanita virosa", "aliases": ["殺しの天使"] },
    "toxicity": "deadly_toxic",
    "season": { "start_month": 6, "end_month": 10 },
    "habitat": ["広葉樹林", "針葉樹林"],
    "regions": ["北海道", "本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "純白の美しい外見に反して猛毒を持つ。タマゴテングタケと同様のアマトキシン類を含み、食べると高い確率で死に至る。「殺しの天使」の異名を持つ。",
    "features": "全体が純白色。傘は円錐形で表面は滑らか。柄は白色で繊維状のささくれがある。基部にふくらみとつぼがある。つばがある。",
    "similar_species": [],
    "caution": "致死性の猛毒。1本で致死量に達する。白いキノコ全般に対して警戒が必要。絶対に採取・食用しないこと。",
    "tree_association": ["ブナ", "ミズナラ", "マツ"],
    "source_url": "https://ja.wikipedia.org/wiki/ドクツルタケ"
  },
  {
    "id": "beni-tengu-take",
    "names": { "ja": "ベニテングタケ", "scientific": "Amanita muscaria" },
    "toxicity": "toxic",
    "season": { "start_month": 7, "end_month": 10 },
    "habitat": ["シラカバ林", "針葉樹林"],
    "regions": ["北海道", "本州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "赤い傘に白いイボが点在する、最も有名なキノコの一つ。イボテン酸・ムッシモールを含む毒キノコで、摂取すると錯乱・幻覚・嘔吐などの中毒症状を起こす。",
    "features": "傘は鮮赤色で白いイボ（外被膜の破片）が点在。柄は白色で基部がふくらむ。つばがある。",
    "similar_species": [],
    "caution": "毒キノコ。摂取すると神経系症状（錯乱・幻覚・嘔吐）を起こす。致死例はまれだが危険。",
    "source_url": "https://ja.wikipedia.org/wiki/ベニテングタケ"
  },
  {
    "id": "kaen-take",
    "names": { "ja": "カエンタケ", "scientific": "Trichoderma cornu-damae" },
    "toxicity": "deadly_toxic",
    "season": { "start_month": 8, "end_month": 10 },
    "habitat": ["広葉樹林", "ナラ枯れの樹木周辺"],
    "regions": ["本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "触るだけでも皮膚がただれる猛毒キノコ。トリコテセン系毒素を含み、摂取すると多臓器不全を引き起こす。近年ナラ枯れの拡大に伴い発生域が広がっている。",
    "features": "手の指のような形で直立する。鮮やかな赤〜オレンジ色。表面は滑らか。通常のキノコとは全く異なる形状。",
    "similar_species": [],
    "caution": "触れるだけで皮膚炎を起こす。絶対に素手で触らないこと。摂取は致死的。見つけても決して触れないこと。",
    "source_url": "https://ja.wikipedia.org/wiki/カエンタケ"
  },
  {
    "id": "tsukiyo-take",
    "names": { "ja": "ツキヨタケ", "scientific": "Omphalotus japonicus" },
    "toxicity": "toxic",
    "season": { "start_month": 9, "end_month": 11 },
    "habitat": ["ブナ林", "広葉樹の枯木"],
    "regions": ["北海道", "本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "日本で最も誤食事故が多い毒キノコ。シイタケ・ヒラタケ・ムキタケと間違えやすい。暗所でひだが青白く発光する特徴がある。イルジンSを含み、激しい嘔吐・下痢を引き起こす。",
    "features": "傘は半円形〜扇形で褐色。ひだは暗所で青白く発光する。柄の断面に黒紫色のシミがある（最重要の識別ポイント）。",
    "similar_species": ["shiitake", "hiratake"],
    "caution": "シイタケ・ヒラタケとの誤食事故が非常に多い。柄を縦に割いて断面に黒いシミがないか必ず確認すること。",
    "tree_association": ["ブナ", "ミズナラ"],
    "source_url": "https://ja.wikipedia.org/wiki/ツキヨタケ"
  },
  {
    "id": "sugi-hiratake",
    "names": { "ja": "スギヒラタケ", "scientific": "Pleurocybella porrigens" },
    "toxicity": "edible_caution",
    "season": { "start_month": 9, "end_month": 11 },
    "habitat": ["スギ林", "針葉樹の枯木"],
    "regions": ["北海道", "本州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "かつては食用とされていたが、2004年に腎機能障害のある人が摂取後に急性脳症で死亡する事故が相次ぎ、現在は食用非推奨とされている。",
    "features": "傘は白色で薄く、扇形〜ヘラ形。柄はほとんどなく、枯木に直接つく。ひだは白色で密。",
    "similar_species": ["hiratake"],
    "caution": "2004年以降、摂取による死亡例が報告されている。腎臓に疾患がある人は絶対に食べないこと。健康な人でも食用は推奨しない。",
    "tree_association": ["スギ"],
    "source_url": "https://ja.wikipedia.org/wiki/スギヒラタケ"
  },
  {
    "id": "nigakuri-take",
    "names": { "ja": "ニガクリタケ", "scientific": "Hypholoma fasciculare" },
    "toxicity": "toxic",
    "season": { "start_month": 4, "end_month": 11 },
    "habitat": ["広葉樹林", "針葉樹林", "枯木・切株"],
    "regions": ["北海道", "本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "広い季節に渡って発生する毒キノコ。ナメコやクリタケと混同されやすく、誤食事故が多い。ファシキュロール類の毒素を含み、消化器系の中毒症状を引き起こす。",
    "features": "傘は黄色〜硫黄色で小型。ひだは黄緑色〜オリーブ色。肉は苦い（味見は吐き出すこと）。束生する。",
    "similar_species": ["nameko", "enokitake"],
    "caution": "ナメコ・クリタケとの誤食に注意。ニガクリタケは肉が苦く、ひだがオリーブ色を帯びる。",
    "tree_association": ["ブナ", "コナラ", "スギ"],
    "source_url": "https://ja.wikipedia.org/wiki/ニガクリタケ"
  },
  {
    "id": "kawara-take",
    "names": { "ja": "カワラタケ", "scientific": "Trametes versicolor" },
    "toxicity": "inedible",
    "season": { "start_month": 1, "end_month": 12 },
    "habitat": ["広葉樹林", "枯木・倒木"],
    "regions": ["北海道", "本州", "四国", "九州"],
    "image_local": "/images/mushrooms/placeholder.svg",
    "images_remote": [],
    "description": "年間を通じて見られる非常にありふれたキノコ。薬用キノコとして研究されているが、硬くて食用にはならない。多様な色の環紋模様が美しい。",
    "features": "傘は半円形で薄く硬い。表面に同心円状の色帯（環紋）がある。色は灰色・褐色・青色など多様。裏面は白色の管孔。",
    "similar_species": [],
    "source_url": "https://ja.wikipedia.org/wiki/カワラタケ"
  }
]
```

- [ ] **Step 2: Write failing tests for data module**

`src/data/mushrooms.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import {
  mushrooms,
  getMushroomById,
  searchMushrooms,
  getMushroomsBySeason,
} from './mushrooms';

describe('mushrooms data', () => {
  it('loads all mushroom entries', () => {
    expect(mushrooms.length).toBe(13);
  });

  it('every entry has required fields', () => {
    for (const m of mushrooms) {
      expect(m.id).toBeTruthy();
      expect(m.names.ja).toBeTruthy();
      expect(m.names.scientific).toBeTruthy();
      expect(m.toxicity).toBeTruthy();
      expect(m.season.start_month).toBeGreaterThanOrEqual(1);
      expect(m.season.end_month).toBeLessThanOrEqual(12);
    }
  });
});

describe('getMushroomById', () => {
  it('returns mushroom for valid id', () => {
    const m = getMushroomById('matsutake');
    expect(m).toBeDefined();
    expect(m!.names.ja).toBe('マツタケ');
  });

  it('returns undefined for invalid id', () => {
    expect(getMushroomById('nonexistent')).toBeUndefined();
  });
});

describe('searchMushrooms', () => {
  it('filters by Japanese name query', () => {
    const results = searchMushrooms({ query: 'マツ' });
    expect(results.some((m) => m.id === 'matsutake')).toBe(true);
  });

  it('filters by scientific name query', () => {
    const results = searchMushrooms({ query: 'Amanita' });
    expect(results.length).toBeGreaterThanOrEqual(2);
  });

  it('filters by alias query', () => {
    const results = searchMushrooms({ query: '松茸' });
    expect(results.some((m) => m.id === 'matsutake')).toBe(true);
  });

  it('filters by toxicity', () => {
    const results = searchMushrooms({ toxicity: ['deadly_toxic'] });
    expect(results.length).toBe(3);
    for (const m of results) {
      expect(m.toxicity).toBe('deadly_toxic');
    }
  });

  it('filters by multiple toxicity values', () => {
    const results = searchMushrooms({ toxicity: ['toxic', 'deadly_toxic'] });
    expect(results.length).toBe(6);
  });

  it('combines query and toxicity filters', () => {
    const results = searchMushrooms({ query: 'テング', toxicity: ['deadly_toxic'] });
    expect(results.some((m) => m.id === 'tamago-tengu-take')).toBe(true);
    expect(results.every((m) => m.toxicity === 'deadly_toxic')).toBe(true);
  });

  it('returns all mushrooms when no filters', () => {
    const results = searchMushrooms({});
    expect(results.length).toBe(13);
  });
});

describe('getMushroomsBySeason', () => {
  it('returns mushrooms available in September', () => {
    const results = getMushroomsBySeason(9);
    expect(results.some((m) => m.id === 'matsutake')).toBe(true);
    expect(results.some((m) => m.id === 'nameko')).toBe(true);
  });

  it('handles season wrapping across year boundary', () => {
    // エノキタケ: season 11-3 (November to March)
    const janResults = getMushroomsBySeason(1);
    expect(janResults.some((m) => m.id === 'enokitake')).toBe(true);

    const decResults = getMushroomsBySeason(12);
    expect(decResults.some((m) => m.id === 'enokitake')).toBe(true);

    // Should NOT appear in summer
    const julResults = getMushroomsBySeason(7);
    expect(julResults.some((m) => m.id === 'enokitake')).toBe(false);
  });

  it('returns year-round mushrooms for any month', () => {
    // カワラタケ: season 1-12
    for (let month = 1; month <= 12; month++) {
      const results = getMushroomsBySeason(month);
      expect(results.some((m) => m.id === 'kawara-take')).toBe(true);
    }
  });
});
```

- [ ] **Step 3: Run tests to verify they fail**

```bash
npm test -- src/data/mushrooms.test.ts
```

Expected: FAIL — module './mushrooms' not found.

- [ ] **Step 4: Implement data module**

`src/data/mushrooms.ts`:
```typescript
import mushroomsRaw from './mushrooms.json';
import type { Mushroom, FilterOptions } from '@/types/mushroom';

export const mushrooms: Mushroom[] = mushroomsRaw as Mushroom[];

const mushroomIndex = new Map<string, Mushroom>(
  mushrooms.map((m) => [m.id, m])
);

export function getMushroomById(id: string): Mushroom | undefined {
  return mushroomIndex.get(id);
}

export function searchMushrooms(filters: FilterOptions): Mushroom[] {
  let results = [...mushrooms];

  if (filters.query) {
    const q = filters.query.toLowerCase();
    results = results.filter(
      (m) =>
        m.names.ja.toLowerCase().includes(q) ||
        m.names.scientific.toLowerCase().includes(q) ||
        m.names.aliases?.some((a) => a.toLowerCase().includes(q))
    );
  }

  if (filters.toxicity && filters.toxicity.length > 0) {
    results = results.filter((m) => filters.toxicity!.includes(m.toxicity));
  }

  if (filters.season) {
    results = results.filter((m) => isInSeason(m, filters.season!));
  }

  if (filters.habitat) {
    const h = filters.habitat.toLowerCase();
    results = results.filter((m) =>
      m.habitat.some((hab) => hab.toLowerCase().includes(h))
    );
  }

  return results;
}

export function getMushroomsBySeason(month: number): Mushroom[] {
  return mushrooms.filter((m) => isInSeason(m, month));
}

function isInSeason(mushroom: Mushroom, month: number): boolean {
  const { start_month, end_month } = mushroom.season;
  if (start_month <= end_month) {
    return month >= start_month && month <= end_month;
  }
  // Season wraps across year boundary (e.g., 11 to 3)
  return month >= start_month || month <= end_month;
}
```

- [ ] **Step 5: Run tests to verify they pass**

```bash
npm test -- src/data/mushrooms.test.ts
```

Expected: All tests pass.

- [ ] **Step 6: Commit**

```bash
git add src/data/
git commit -m "feat: add sample mushroom data (13 species) and data module with search/filter"
```

---

## Task 4: AppContext (Online State & Settings)

**Files:**
- Create: `src/contexts/AppContext.tsx`, `src/contexts/AppContext.test.tsx`

- [ ] **Step 1: Write failing tests for AppContext reducer**

`src/contexts/AppContext.test.tsx`:
```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { AppProvider, useApp } from './AppContext';
import type { ReactNode } from 'react';

function wrapper({ children }: { children: ReactNode }) {
  return <AppProvider>{children}</AppProvider>;
}

describe('AppContext', () => {
  beforeEach(() => {
    localStorage.clear();
    vi.restoreAllMocks();
  });

  it('initializes with default values', () => {
    const { result } = renderHook(() => useApp(), { wrapper });
    expect(result.current.state.isOnline).toBe(true);
    expect(result.current.state.apiKey).toBeNull();
    expect(result.current.state.preferredRegions).toEqual([]);
  });

  it('restores API key from localStorage', () => {
    localStorage.setItem('anthropic_api_key', 'sk-test-key');
    const { result } = renderHook(() => useApp(), { wrapper });
    expect(result.current.state.apiKey).toBe('sk-test-key');
  });

  it('updates API key and persists to localStorage', () => {
    const { result } = renderHook(() => useApp(), { wrapper });
    act(() => {
      result.current.dispatch({ type: 'SET_API_KEY', payload: 'sk-new-key' });
    });
    expect(result.current.state.apiKey).toBe('sk-new-key');
    expect(localStorage.getItem('anthropic_api_key')).toBe('sk-new-key');
  });

  it('clears API key from localStorage when set to null', () => {
    localStorage.setItem('anthropic_api_key', 'sk-old');
    const { result } = renderHook(() => useApp(), { wrapper });
    act(() => {
      result.current.dispatch({ type: 'SET_API_KEY', payload: null });
    });
    expect(result.current.state.apiKey).toBeNull();
    expect(localStorage.getItem('anthropic_api_key')).toBeNull();
  });

  it('updates online status', () => {
    const { result } = renderHook(() => useApp(), { wrapper });
    act(() => {
      result.current.dispatch({ type: 'SET_ONLINE', payload: false });
    });
    expect(result.current.state.isOnline).toBe(false);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
npm test -- src/contexts/AppContext.test.tsx
```

Expected: FAIL — module './AppContext' not found.

- [ ] **Step 3: Implement AppContext**

`src/contexts/AppContext.tsx`:
```typescript
'use client';

import {
  createContext,
  useContext,
  useReducer,
  useEffect,
  type ReactNode,
  type Dispatch,
} from 'react';

interface AppState {
  isOnline: boolean;
  apiKey: string | null;
  preferredRegions: string[];
  theme: 'light' | 'dark' | 'system';
}

type AppAction =
  | { type: 'SET_ONLINE'; payload: boolean }
  | { type: 'SET_API_KEY'; payload: string | null }
  | { type: 'SET_REGIONS'; payload: string[] }
  | { type: 'SET_THEME'; payload: 'light' | 'dark' | 'system' };

function getInitialState(): AppState {
  const apiKey = typeof window !== 'undefined'
    ? localStorage.getItem('anthropic_api_key')
    : null;

  return {
    isOnline: typeof navigator !== 'undefined' ? navigator.onLine : true,
    apiKey,
    preferredRegions: [],
    theme: 'system',
  };
}

function appReducer(state: AppState, action: AppAction): AppState {
  switch (action.type) {
    case 'SET_ONLINE':
      return { ...state, isOnline: action.payload };
    case 'SET_API_KEY':
      if (action.payload) {
        localStorage.setItem('anthropic_api_key', action.payload);
      } else {
        localStorage.removeItem('anthropic_api_key');
      }
      return { ...state, apiKey: action.payload };
    case 'SET_REGIONS':
      return { ...state, preferredRegions: action.payload };
    case 'SET_THEME':
      return { ...state, theme: action.payload };
    default:
      return state;
  }
}

interface AppContextValue {
  state: AppState;
  dispatch: Dispatch<AppAction>;
}

const AppContext = createContext<AppContextValue | null>(null);

export function AppProvider({ children }: { children: ReactNode }) {
  const [state, dispatch] = useReducer(appReducer, undefined, getInitialState);

  useEffect(() => {
    const handleOnline = () => dispatch({ type: 'SET_ONLINE', payload: true });
    const handleOffline = () => dispatch({ type: 'SET_ONLINE', payload: false });

    window.addEventListener('online', handleOnline);
    window.addEventListener('offline', handleOffline);
    return () => {
      window.removeEventListener('online', handleOnline);
      window.removeEventListener('offline', handleOffline);
    };
  }, []);

  return (
    <AppContext.Provider value={{ state, dispatch }}>
      {children}
    </AppContext.Provider>
  );
}

export function useApp(): AppContextValue {
  const context = useContext(AppContext);
  if (!context) {
    throw new Error('useApp must be used within AppProvider');
  }
  return context;
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npm test -- src/contexts/AppContext.test.tsx
```

Expected: All tests pass.

- [ ] **Step 5: Commit**

```bash
git add src/contexts/
git commit -m "feat: add AppContext with online state tracking and localStorage sync"
```

---

## Task 5: UI Foundation Components

**Files:**
- Create: `src/components/ui/Button.tsx`, `src/components/ui/LoadingSpinner.tsx`, `src/components/zukan/ToxicityBadge.tsx`, `src/components/zukan/ToxicityBadge.test.tsx`, `src/components/zukan/SeasonBar.tsx`, `src/components/zukan/SeasonBar.test.tsx`

- [ ] **Step 1: Create Button component**

`src/components/ui/Button.tsx`:
```typescript
'use client';

import { type ButtonHTMLAttributes } from 'react';

interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
}

const variants = {
  primary: 'bg-forest-500 hover:bg-forest-400 text-white',
  secondary: 'bg-forest-800 hover:bg-forest-700 text-forest-100 border border-forest-600',
  ghost: 'hover:bg-forest-800 text-forest-200',
};

const sizes = {
  sm: 'px-3 py-1.5 text-sm',
  md: 'px-4 py-2 text-base',
  lg: 'px-6 py-3 text-lg',
};

export function Button({
  variant = 'primary',
  size = 'md',
  className = '',
  ...props
}: ButtonProps) {
  return (
    <button
      className={`rounded-lg font-medium transition-colors disabled:opacity-50 disabled:cursor-not-allowed ${variants[variant]} ${sizes[size]} ${className}`}
      {...props}
    />
  );
}
```

- [ ] **Step 2: Create LoadingSpinner component**

`src/components/ui/LoadingSpinner.tsx`:
```typescript
import { UI_TEXT } from '@/constants/ui-text';

export function LoadingSpinner() {
  return (
    <div className="flex items-center justify-center p-8" role="status">
      <div className="h-8 w-8 animate-spin rounded-full border-2 border-forest-300 border-t-transparent" />
      <span className="sr-only">{UI_TEXT.common.loading}</span>
    </div>
  );
}
```

- [ ] **Step 3: Write failing tests for ToxicityBadge**

`src/components/zukan/ToxicityBadge.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { ToxicityBadge } from './ToxicityBadge';
import { TOXICITY_CONFIG } from '@/constants/toxicity';
import type { Toxicity } from '@/types/mushroom';

describe('ToxicityBadge', () => {
  const allToxicities: Toxicity[] = [
    'edible', 'edible_caution', 'inedible', 'toxic', 'deadly_toxic',
  ];

  it.each(allToxicities)('renders correct label for %s', (toxicity) => {
    render(<ToxicityBadge toxicity={toxicity} />);
    expect(screen.getByText(TOXICITY_CONFIG[toxicity].label)).toBeInTheDocument();
  });

  it('renders larger badge for toxic and deadly_toxic', () => {
    const { container: toxicContainer } = render(<ToxicityBadge toxicity="toxic" />);
    const { container: edibleContainer } = render(<ToxicityBadge toxicity="edible" />);

    const toxicBadge = toxicContainer.firstChild as HTMLElement;
    const edibleBadge = edibleContainer.firstChild as HTMLElement;

    expect(toxicBadge.className).toContain('text-sm');
    expect(edibleBadge.className).toContain('text-xs');
  });

  it('includes icon in the badge', () => {
    render(<ToxicityBadge toxicity="deadly_toxic" />);
    expect(screen.getByText(/☠/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 4: Run tests to verify they fail**

```bash
npm test -- src/components/zukan/ToxicityBadge.test.tsx
```

Expected: FAIL.

- [ ] **Step 5: Implement ToxicityBadge**

`src/components/zukan/ToxicityBadge.tsx`:
```typescript
import { TOXICITY_CONFIG } from '@/constants/toxicity';
import type { Toxicity } from '@/types/mushroom';

interface ToxicityBadgeProps {
  toxicity: Toxicity;
}

export function ToxicityBadge({ toxicity }: ToxicityBadgeProps) {
  const config = TOXICITY_CONFIG[toxicity];
  const isDangerous = toxicity === 'toxic' || toxicity === 'deadly_toxic';

  return (
    <span
      className={`inline-flex items-center gap-1 rounded-full px-2 py-0.5 font-bold text-white ${config.color} ${isDangerous ? 'text-sm' : 'text-xs'}`}
    >
      <span>{config.icon}</span>
      <span>{config.label}</span>
    </span>
  );
}
```

- [ ] **Step 6: Run tests to verify they pass**

```bash
npm test -- src/components/zukan/ToxicityBadge.test.tsx
```

Expected: All tests pass.

- [ ] **Step 7: Write failing tests for SeasonBar**

`src/components/zukan/SeasonBar.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { SeasonBar } from './SeasonBar';

describe('SeasonBar', () => {
  it('renders 12 month cells', () => {
    render(<SeasonBar startMonth={4} endMonth={6} />);
    const cells = screen.getAllByRole('cell');
    expect(cells).toHaveLength(12);
  });

  it('highlights months within season (normal range)', () => {
    const { container } = render(<SeasonBar startMonth={9} endMonth={11} />);
    const cells = container.querySelectorAll('[data-month]');
    // Months 9, 10, 11 should be active
    cells.forEach((cell) => {
      const month = Number(cell.getAttribute('data-month'));
      if (month >= 9 && month <= 11) {
        expect(cell.className).toContain('bg-forest-300');
      } else {
        expect(cell.className).not.toContain('bg-forest-300');
      }
    });
  });

  it('highlights months across year boundary', () => {
    // エノキタケ: November to March
    const { container } = render(<SeasonBar startMonth={11} endMonth={3} />);
    const cells = container.querySelectorAll('[data-month]');
    cells.forEach((cell) => {
      const month = Number(cell.getAttribute('data-month'));
      const isActive = month >= 11 || month <= 3;
      if (isActive) {
        expect(cell.className).toContain('bg-forest-300');
      } else {
        expect(cell.className).not.toContain('bg-forest-300');
      }
    });
  });
});
```

- [ ] **Step 8: Run tests to verify they fail**

```bash
npm test -- src/components/zukan/SeasonBar.test.tsx
```

Expected: FAIL.

- [ ] **Step 9: Implement SeasonBar**

`src/components/zukan/SeasonBar.tsx`:
```typescript
import { UI_TEXT } from '@/constants/ui-text';

interface SeasonBarProps {
  startMonth: number;
  endMonth: number;
}

function isMonthActive(month: number, startMonth: number, endMonth: number): boolean {
  if (startMonth <= endMonth) {
    return month >= startMonth && month <= endMonth;
  }
  return month >= startMonth || month <= endMonth;
}

export function SeasonBar({ startMonth, endMonth }: SeasonBarProps) {
  return (
    <table className="w-full" role="table">
      <thead>
        <tr>
          {UI_TEXT.months.map((label, i) => (
            <th key={i} className="text-[10px] text-forest-400 font-normal px-0.5">
              {label.replace('月', '')}
            </th>
          ))}
        </tr>
      </thead>
      <tbody>
        <tr>
          {Array.from({ length: 12 }, (_, i) => {
            const month = i + 1;
            const active = isMonthActive(month, startMonth, endMonth);
            return (
              <td
                key={month}
                role="cell"
                data-month={month}
                className={`h-3 rounded-sm ${active ? 'bg-forest-300' : 'bg-forest-800'}`}
              />
            );
          })}
        </tr>
      </tbody>
    </table>
  );
}
```

- [ ] **Step 10: Run tests to verify they pass**

```bash
npm test -- src/components/zukan/SeasonBar.test.tsx
```

Expected: All tests pass.

- [ ] **Step 11: Commit**

```bash
git add src/components/ui/ src/components/zukan/ToxicityBadge.tsx src/components/zukan/ToxicityBadge.test.tsx src/components/zukan/SeasonBar.tsx src/components/zukan/SeasonBar.test.tsx
git commit -m "feat: add UI foundation components (Button, LoadingSpinner, ToxicityBadge, SeasonBar)"
```

---

## Task 6: Layout Components & Root Layout

**Files:**
- Create: `src/components/layout/BottomNav.tsx`, `src/components/layout/BottomNav.test.tsx`, `src/components/layout/OfflineBanner.tsx`, `src/components/layout/OfflineBanner.test.tsx`, `src/components/layout/PageHeader.tsx`, `src/app/layout.tsx`

- [ ] **Step 1: Write failing tests for BottomNav**

`src/components/layout/BottomNav.test.tsx`:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import { BottomNav } from './BottomNav';
import { UI_TEXT } from '@/constants/ui-text';

vi.mock('next/navigation', () => ({
  usePathname: () => '/zukan',
}));

describe('BottomNav', () => {
  it('renders all 5 navigation tabs', () => {
    render(<BottomNav />);
    expect(screen.getByText(UI_TEXT.nav.zukan)).toBeInTheDocument();
    expect(screen.getByText(UI_TEXT.nav.identify)).toBeInTheDocument();
    expect(screen.getByText(UI_TEXT.nav.plan)).toBeInTheDocument();
    expect(screen.getByText(UI_TEXT.nav.records)).toBeInTheDocument();
    expect(screen.getByText(UI_TEXT.nav.settings)).toBeInTheDocument();
  });

  it('highlights the active tab based on pathname', () => {
    render(<BottomNav />);
    const zukanTab = screen.getByText(UI_TEXT.nav.zukan).closest('a');
    expect(zukanTab?.className).toContain('text-forest-300');
  });

  it('renders navigation links with correct hrefs', () => {
    render(<BottomNav />);
    const links = screen.getAllByRole('link');
    const hrefs = links.map((l) => l.getAttribute('href'));
    expect(hrefs).toContain('/zukan');
    expect(hrefs).toContain('/identify');
    expect(hrefs).toContain('/plan');
    expect(hrefs).toContain('/records');
    expect(hrefs).toContain('/settings');
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
npm test -- src/components/layout/BottomNav.test.tsx
```

Expected: FAIL.

- [ ] **Step 3: Implement BottomNav**

`src/components/layout/BottomNav.tsx`:
```typescript
'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { UI_TEXT } from '@/constants/ui-text';

const NAV_ITEMS = [
  { href: '/zukan', label: UI_TEXT.nav.zukan, icon: '📖' },
  { href: '/identify', label: UI_TEXT.nav.identify, icon: '🔍' },
  { href: '/plan', label: UI_TEXT.nav.plan, icon: '🗺' },
  { href: '/records', label: UI_TEXT.nav.records, icon: '📝' },
  { href: '/settings', label: UI_TEXT.nav.settings, icon: '⚙' },
] as const;

export function BottomNav() {
  const pathname = usePathname();

  return (
    <nav className="fixed bottom-0 left-0 right-0 z-50 border-t border-forest-700 bg-forest-900">
      <div className="mx-auto flex max-w-lg items-center justify-around">
        {NAV_ITEMS.map((item) => {
          const isActive = pathname.startsWith(item.href);
          return (
            <Link
              key={item.href}
              href={item.href}
              className={`flex flex-1 flex-col items-center gap-0.5 py-2 text-xs transition-colors ${
                isActive ? 'text-forest-300' : 'text-forest-500'
              }`}
            >
              <span className="text-lg">{item.icon}</span>
              <span>{item.label}</span>
            </Link>
          );
        })}
      </div>
    </nav>
  );
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npm test -- src/components/layout/BottomNav.test.tsx
```

Expected: All tests pass.

- [ ] **Step 5: Write failing tests for OfflineBanner**

`src/components/layout/OfflineBanner.test.tsx`:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, act } from '@testing-library/react';
import { OfflineBanner } from './OfflineBanner';
import { AppProvider } from '@/contexts/AppContext';
import type { ReactNode } from 'react';

function wrapper({ children }: { children: ReactNode }) {
  return <AppProvider>{children}</AppProvider>;
}

describe('OfflineBanner', () => {
  beforeEach(() => {
    localStorage.clear();
    Object.defineProperty(navigator, 'onLine', {
      writable: true,
      value: true,
    });
  });

  it('does not show banner when online', () => {
    render(<OfflineBanner />, { wrapper });
    expect(screen.queryByText(/オフライン/)).not.toBeInTheDocument();
  });

  it('shows banner when offline event fires', () => {
    render(<OfflineBanner />, { wrapper });
    act(() => {
      Object.defineProperty(navigator, 'onLine', { value: false });
      window.dispatchEvent(new Event('offline'));
    });
    expect(screen.getByText(/オフライン/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 6: Run tests to verify they fail**

```bash
npm test -- src/components/layout/OfflineBanner.test.tsx
```

Expected: FAIL.

- [ ] **Step 7: Implement OfflineBanner**

`src/components/layout/OfflineBanner.tsx`:
```typescript
'use client';

import { useState, useEffect } from 'react';
import { useApp } from '@/contexts/AppContext';
import { UI_TEXT } from '@/constants/ui-text';

export function OfflineBanner() {
  const { state } = useApp();
  const [showRestored, setShowRestored] = useState(false);
  const [wasOffline, setWasOffline] = useState(false);

  useEffect(() => {
    if (!state.isOnline) {
      setWasOffline(true);
    } else if (wasOffline) {
      setShowRestored(true);
      const timer = setTimeout(() => setShowRestored(false), 3000);
      setWasOffline(false);
      return () => clearTimeout(timer);
    }
  }, [state.isOnline, wasOffline]);

  if (!state.isOnline) {
    return (
      <div className="fixed top-0 left-0 right-0 z-50 bg-yellow-600 px-4 py-2 text-center text-sm font-medium text-white">
        ⚡ {UI_TEXT.common.offline}
      </div>
    );
  }

  if (showRestored) {
    return (
      <div className="fixed top-0 left-0 right-0 z-50 bg-green-600 px-4 py-2 text-center text-sm font-medium text-white">
        ✓ {UI_TEXT.common.onlineRestored}
      </div>
    );
  }

  return null;
}
```

- [ ] **Step 8: Run tests to verify they pass**

```bash
npm test -- src/components/layout/OfflineBanner.test.tsx
```

Expected: All tests pass.

- [ ] **Step 9: Create PageHeader component**

`src/components/layout/PageHeader.tsx`:
```typescript
'use client';

import { useRouter } from 'next/navigation';
import { UI_TEXT } from '@/constants/ui-text';

interface PageHeaderProps {
  title: string;
  showBack?: boolean;
}

export function PageHeader({ title, showBack = false }: PageHeaderProps) {
  const router = useRouter();

  return (
    <header className="sticky top-0 z-40 border-b border-forest-700 bg-forest-900 px-4 py-3">
      <div className="mx-auto flex max-w-lg items-center gap-3">
        {showBack && (
          <button
            onClick={() => router.back()}
            className="text-forest-300 hover:text-forest-200 transition-colors"
            aria-label={UI_TEXT.common.back}
          >
            ← 
          </button>
        )}
        <h1 className="text-lg font-bold text-forest-50">{title}</h1>
      </div>
    </header>
  );
}
```

- [ ] **Step 10: Create root layout**

`src/app/layout.tsx`:
```typescript
import type { Metadata, Viewport } from 'next';
import { AppProvider } from '@/contexts/AppContext';
import { BottomNav } from '@/components/layout/BottomNav';
import { OfflineBanner } from '@/components/layout/OfflineBanner';
import './globals.css';

export const metadata: Metadata = {
  title: 'MycoNote - キノコ採取・観察ハンドブック',
  description: 'キノコの採取・観察をサポートするオフライン対応ハンドブック',
  manifest: '/manifest.json',
};

export const viewport: Viewport = {
  themeColor: '#0a1504',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ja">
      <body className="bg-forest-950 text-forest-50">
        <AppProvider>
          <OfflineBanner />
          <main className="mx-auto max-w-lg pb-16">{children}</main>
          <BottomNav />
        </AppProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 11: Create home page redirect**

`src/app/page.tsx`:
```typescript
import { redirect } from 'next/navigation';

export default function Home() {
  redirect('/zukan');
}
```

- [ ] **Step 12: Commit**

```bash
git add src/components/layout/ src/app/layout.tsx src/app/page.tsx src/app/globals.css
git commit -m "feat: add layout components (BottomNav, OfflineBanner, PageHeader) and root layout"
```

---

## Task 7: Zukan List Page

**Files:**
- Create: `src/components/zukan/MushroomCard.tsx`, `src/components/zukan/MushroomCard.test.tsx`, `src/components/zukan/SearchFilter.tsx`, `src/components/zukan/SearchFilter.test.tsx`, `src/app/zukan/page.tsx`

- [ ] **Step 1: Write failing tests for MushroomCard**

`src/components/zukan/MushroomCard.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MushroomCard } from './MushroomCard';
import type { Mushroom } from '@/types/mushroom';

const mockMushroom: Mushroom = {
  id: 'test-kinoko',
  names: { ja: 'テストキノコ', scientific: 'Testus fungus' },
  toxicity: 'edible',
  season: { start_month: 4, end_month: 6 },
  habitat: ['広葉樹林'],
  regions: ['本州'],
  image_local: '/images/mushrooms/placeholder.svg',
  images_remote: [],
  description: 'テスト用のキノコです',
  features: 'テスト特徴',
  similar_species: [],
};

describe('MushroomCard', () => {
  it('renders Japanese name', () => {
    render(<MushroomCard mushroom={mockMushroom} />);
    expect(screen.getByText('テストキノコ')).toBeInTheDocument();
  });

  it('renders toxicity badge', () => {
    render(<MushroomCard mushroom={mockMushroom} />);
    expect(screen.getByText('食用')).toBeInTheDocument();
  });

  it('links to detail page', () => {
    render(<MushroomCard mushroom={mockMushroom} />);
    const link = screen.getByRole('link');
    expect(link.getAttribute('href')).toBe('/zukan/test-kinoko');
  });

  it('renders deadly_toxic badge prominently', () => {
    const toxicMushroom = { ...mockMushroom, toxicity: 'deadly_toxic' as const };
    render(<MushroomCard mushroom={toxicMushroom} />);
    expect(screen.getByText('猛毒')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
npm test -- src/components/zukan/MushroomCard.test.tsx
```

Expected: FAIL.

- [ ] **Step 3: Implement MushroomCard**

`src/components/zukan/MushroomCard.tsx`:
```typescript
import Link from 'next/link';
import Image from 'next/image';
import { ToxicityBadge } from './ToxicityBadge';
import type { Mushroom } from '@/types/mushroom';

interface MushroomCardProps {
  mushroom: Mushroom;
}

export function MushroomCard({ mushroom }: MushroomCardProps) {
  return (
    <Link
      href={`/zukan/${mushroom.id}`}
      className="block overflow-hidden rounded-lg border border-forest-700 bg-forest-800 transition-colors hover:bg-forest-700"
    >
      <div className="relative aspect-square">
        <Image
          src={mushroom.image_local}
          alt={mushroom.names.ja}
          fill
          className="object-cover"
          sizes="(max-width: 768px) 50vw, 200px"
        />
      </div>
      <div className="p-2">
        <div className="mb-1 text-sm font-bold text-forest-50">
          {mushroom.names.ja}
        </div>
        <ToxicityBadge toxicity={mushroom.toxicity} />
      </div>
    </Link>
  );
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npm test -- src/components/zukan/MushroomCard.test.tsx
```

Expected: All tests pass.

- [ ] **Step 5: Write failing tests for SearchFilter**

`src/components/zukan/SearchFilter.test.tsx`:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { SearchFilter } from './SearchFilter';
import { UI_TEXT } from '@/constants/ui-text';
import type { FilterOptions } from '@/types/mushroom';

describe('SearchFilter', () => {
  it('renders search input', () => {
    render(<SearchFilter filters={{}} onFilterChange={() => {}} />);
    expect(screen.getByPlaceholderText(UI_TEXT.zukan.searchPlaceholder)).toBeInTheDocument();
  });

  it('calls onFilterChange when search text changes', async () => {
    const user = userEvent.setup();
    const onChange = vi.fn();
    render(<SearchFilter filters={{}} onFilterChange={onChange} />);
    const input = screen.getByPlaceholderText(UI_TEXT.zukan.searchPlaceholder);
    await user.type(input, 'マツ');
    expect(onChange).toHaveBeenCalledWith(expect.objectContaining({ query: 'マツ' }));
  });

  it('renders toxicity filter buttons', () => {
    render(<SearchFilter filters={{}} onFilterChange={() => {}} />);
    expect(screen.getByText('食用')).toBeInTheDocument();
    expect(screen.getByText('毒')).toBeInTheDocument();
    expect(screen.getByText('猛毒')).toBeInTheDocument();
  });

  it('calls onFilterChange when toxicity filter is toggled', async () => {
    const user = userEvent.setup();
    const onChange = vi.fn();
    render(<SearchFilter filters={{}} onFilterChange={onChange} />);
    await user.click(screen.getByText('食用'));
    expect(onChange).toHaveBeenCalledWith(
      expect.objectContaining({ toxicity: expect.arrayContaining(['edible']) })
    );
  });
});
```

- [ ] **Step 6: Run tests to verify they fail**

```bash
npm test -- src/components/zukan/SearchFilter.test.tsx
```

Expected: FAIL.

- [ ] **Step 7: Implement SearchFilter**

`src/components/zukan/SearchFilter.tsx`:
```typescript
'use client';

import { UI_TEXT } from '@/constants/ui-text';
import { TOXICITY_CONFIG } from '@/constants/toxicity';
import type { FilterOptions, Toxicity } from '@/types/mushroom';

interface SearchFilterProps {
  filters: FilterOptions;
  onFilterChange: (filters: FilterOptions) => void;
}

const TOXICITY_FILTERS: { value: Toxicity; label: string }[] = [
  { value: 'edible', label: TOXICITY_CONFIG.edible.label },
  { value: 'edible_caution', label: TOXICITY_CONFIG.edible_caution.label },
  { value: 'inedible', label: TOXICITY_CONFIG.inedible.label },
  { value: 'toxic', label: TOXICITY_CONFIG.toxic.label },
  { value: 'deadly_toxic', label: TOXICITY_CONFIG.deadly_toxic.label },
];

export function SearchFilter({ filters, onFilterChange }: SearchFilterProps) {
  const handleQueryChange = (query: string) => {
    onFilterChange({ ...filters, query: query || undefined });
  };

  const handleToxicityToggle = (toxicity: Toxicity) => {
    const current = filters.toxicity || [];
    const updated = current.includes(toxicity)
      ? current.filter((t) => t !== toxicity)
      : [...current, toxicity];
    onFilterChange({ ...filters, toxicity: updated.length > 0 ? updated : undefined });
  };

  return (
    <div className="space-y-3 px-4 py-3">
      <input
        type="text"
        placeholder={UI_TEXT.zukan.searchPlaceholder}
        value={filters.query || ''}
        onChange={(e) => handleQueryChange(e.target.value)}
        className="w-full rounded-lg border border-forest-600 bg-forest-800 px-4 py-2 text-forest-50 placeholder-forest-500 focus:border-forest-300 focus:outline-none"
      />
      <div className="flex flex-wrap gap-2">
        {TOXICITY_FILTERS.map(({ value, label }) => {
          const isActive = filters.toxicity?.includes(value);
          return (
            <button
              key={value}
              onClick={() => handleToxicityToggle(value)}
              className={`rounded-full px-3 py-1 text-xs font-medium transition-colors ${
                isActive
                  ? `${TOXICITY_CONFIG[value].color} text-white`
                  : 'bg-forest-800 text-forest-400 border border-forest-600'
              }`}
            >
              {label}
            </button>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 8: Run tests to verify they pass**

```bash
npm test -- src/components/zukan/SearchFilter.test.tsx
```

Expected: All tests pass.

- [ ] **Step 9: Create Zukan list page**

`src/app/zukan/page.tsx`:
```typescript
'use client';

import { useState, useMemo } from 'react';
import { searchMushrooms } from '@/data/mushrooms';
import { MushroomCard } from '@/components/zukan/MushroomCard';
import { SearchFilter } from '@/components/zukan/SearchFilter';
import { PageHeader } from '@/components/layout/PageHeader';
import { UI_TEXT } from '@/constants/ui-text';
import type { FilterOptions } from '@/types/mushroom';

export default function ZukanPage() {
  const [filters, setFilters] = useState<FilterOptions>({});

  const filteredMushrooms = useMemo(
    () => searchMushrooms(filters),
    [filters]
  );

  return (
    <div>
      <PageHeader title={UI_TEXT.zukan.title} />
      <SearchFilter filters={filters} onFilterChange={setFilters} />
      {filteredMushrooms.length === 0 ? (
        <p className="px-4 py-8 text-center text-forest-400">
          {UI_TEXT.zukan.noResults}
        </p>
      ) : (
        <div className="grid grid-cols-2 gap-3 px-4 pb-4">
          {filteredMushrooms.map((mushroom) => (
            <MushroomCard key={mushroom.id} mushroom={mushroom} />
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 10: Verify in browser**

```bash
npm run dev
```

ブラウザで http://localhost:3000/zukan にアクセスし、以下を確認:
- 13種がグリッド表示される
- 検索ボックスに入力するとフィルターされる
- 毒性フィルターボタンが動作する

- [ ] **Step 11: Commit**

```bash
git add src/components/zukan/MushroomCard.tsx src/components/zukan/MushroomCard.test.tsx src/components/zukan/SearchFilter.tsx src/components/zukan/SearchFilter.test.tsx src/app/zukan/
git commit -m "feat: add zukan list page with mushroom grid, search, and toxicity filter"
```

---

## Task 8: Zukan Detail Page

**Files:**
- Create: `src/components/zukan/MushroomDetail.tsx`, `src/components/zukan/MushroomDetail.test.tsx`, `src/app/zukan/[id]/page.tsx`

- [ ] **Step 1: Write failing tests for MushroomDetail**

`src/components/zukan/MushroomDetail.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MushroomDetail } from './MushroomDetail';
import type { Mushroom } from '@/types/mushroom';

const mockMushroom: Mushroom = {
  id: 'test-kinoko',
  names: { ja: 'テストキノコ', scientific: 'Testus fungus', aliases: ['別名A'] },
  toxicity: 'toxic',
  season: { start_month: 9, end_month: 11 },
  habitat: ['広葉樹林', 'ブナ林'],
  regions: ['本州', '四国'],
  image_local: '/images/mushrooms/placeholder.svg',
  images_remote: [],
  description: 'これはテスト用のキノコです。',
  features: 'テスト用の形態的特徴です。',
  similar_species: ['shiitake'],
  caution: '注意：これは毒キノコです。',
  tree_association: ['ブナ', 'ナラ'],
};

describe('MushroomDetail', () => {
  it('renders Japanese and scientific name', () => {
    render(<MushroomDetail mushroom={mockMushroom} />);
    expect(screen.getByText('テストキノコ')).toBeInTheDocument();
    expect(screen.getByText('Testus fungus')).toBeInTheDocument();
  });

  it('renders toxicity badge', () => {
    render(<MushroomDetail mushroom={mockMushroom} />);
    expect(screen.getByText('毒')).toBeInTheDocument();
  });

  it('renders caution box for toxic mushrooms', () => {
    render(<MushroomDetail mushroom={mockMushroom} />);
    expect(screen.getByText(/注意：これは毒キノコです/)).toBeInTheDocument();
  });

  it('does not render caution box when caution is absent', () => {
    const safeMushroom = { ...mockMushroom, toxicity: 'edible' as const, caution: undefined };
    render(<MushroomDetail mushroom={safeMushroom} />);
    expect(screen.queryByText(/注意事項/)).not.toBeInTheDocument();
  });

  it('renders season bar', () => {
    const { container } = render(<MushroomDetail mushroom={mockMushroom} />);
    expect(container.querySelector('table')).toBeInTheDocument();
  });

  it('renders habitat list', () => {
    render(<MushroomDetail mushroom={mockMushroom} />);
    expect(screen.getByText(/広葉樹林/)).toBeInTheDocument();
    expect(screen.getByText(/ブナ林/)).toBeInTheDocument();
  });

  it('renders similar species links', () => {
    render(<MushroomDetail mushroom={mockMushroom} />);
    const link = screen.getByRole('link', { name: /シイタケ/ });
    expect(link.getAttribute('href')).toBe('/zukan/shiitake');
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
npm test -- src/components/zukan/MushroomDetail.test.tsx
```

Expected: FAIL.

- [ ] **Step 3: Implement MushroomDetail**

`src/components/zukan/MushroomDetail.tsx`:
```typescript
'use client';

import Image from 'next/image';
import Link from 'next/link';
import { ToxicityBadge } from './ToxicityBadge';
import { SeasonBar } from './SeasonBar';
import { getMushroomById } from '@/data/mushrooms';
import { UI_TEXT } from '@/constants/ui-text';
import type { Mushroom } from '@/types/mushroom';

interface MushroomDetailProps {
  mushroom: Mushroom;
}

export function MushroomDetail({ mushroom }: MushroomDetailProps) {
  return (
    <div className="space-y-4 px-4 pb-8">
      {/* Hero image */}
      <div className="relative aspect-video overflow-hidden rounded-lg">
        <Image
          src={mushroom.image_local}
          alt={mushroom.names.ja}
          fill
          className="object-cover"
          sizes="(max-width: 768px) 100vw, 512px"
          priority
        />
      </div>

      {/* Name and toxicity */}
      <div>
        <div className="flex items-center gap-3">
          <h2 className="text-2xl font-bold text-forest-50">{mushroom.names.ja}</h2>
          <ToxicityBadge toxicity={mushroom.toxicity} />
        </div>
        <p className="mt-1 text-sm italic text-forest-400">{mushroom.names.scientific}</p>
        {mushroom.names.aliases && mushroom.names.aliases.length > 0 && (
          <p className="text-sm text-forest-400">
            別名: {mushroom.names.aliases.join('、')}
          </p>
        )}
      </div>

      {/* Caution box (for toxic mushrooms) */}
      {mushroom.caution && (
        <div className="rounded-lg border-2 border-red-500 bg-red-950/50 p-4">
          <h3 className="mb-1 font-bold text-red-400">⚠ {UI_TEXT.zukan.caution}</h3>
          <p className="text-sm text-red-200">{mushroom.caution}</p>
        </div>
      )}

      {/* Description */}
      <section>
        <h3 className="mb-1 text-sm font-bold text-forest-300">{UI_TEXT.zukan.description}</h3>
        <p className="text-sm text-forest-200">{mushroom.description}</p>
      </section>

      {/* Features */}
      <section>
        <h3 className="mb-1 text-sm font-bold text-forest-300">{UI_TEXT.zukan.features}</h3>
        <p className="text-sm text-forest-200">{mushroom.features}</p>
      </section>

      {/* Season */}
      <section>
        <h3 className="mb-2 text-sm font-bold text-forest-300">{UI_TEXT.zukan.season}</h3>
        <SeasonBar startMonth={mushroom.season.start_month} endMonth={mushroom.season.end_month} />
      </section>

      {/* Habitat */}
      <section>
        <h3 className="mb-1 text-sm font-bold text-forest-300">{UI_TEXT.zukan.habitat}</h3>
        <div className="flex flex-wrap gap-2">
          {mushroom.habitat.map((h) => (
            <span key={h} className="rounded-full bg-forest-800 px-3 py-1 text-xs text-forest-200">
              {h}
            </span>
          ))}
        </div>
      </section>

      {/* Regions */}
      <section>
        <h3 className="mb-1 text-sm font-bold text-forest-300">{UI_TEXT.zukan.regions}</h3>
        <p className="text-sm text-forest-200">{mushroom.regions.join('、')}</p>
      </section>

      {/* Tree association */}
      {mushroom.tree_association && mushroom.tree_association.length > 0 && (
        <section>
          <h3 className="mb-1 text-sm font-bold text-forest-300">{UI_TEXT.zukan.treeAssociation}</h3>
          <p className="text-sm text-forest-200">{mushroom.tree_association.join('、')}</p>
        </section>
      )}

      {/* Similar species */}
      {mushroom.similar_species.length > 0 && (
        <section>
          <h3 className="mb-2 text-sm font-bold text-forest-300">{UI_TEXT.zukan.similarSpecies}</h3>
          <div className="space-y-2">
            {mushroom.similar_species.map((speciesId) => {
              const species = getMushroomById(speciesId);
              if (!species) return null;
              return (
                <Link
                  key={speciesId}
                  href={`/zukan/${speciesId}`}
                  className="flex items-center gap-3 rounded-lg border border-forest-700 bg-forest-800 p-3 transition-colors hover:bg-forest-700"
                >
                  <div className="relative h-12 w-12 flex-shrink-0 overflow-hidden rounded">
                    <Image
                      src={species.image_local}
                      alt={species.names.ja}
                      fill
                      className="object-cover"
                      sizes="48px"
                    />
                  </div>
                  <div className="flex-1">
                    <span className="text-sm font-medium text-forest-100">
                      {species.names.ja}
                    </span>
                  </div>
                  <ToxicityBadge toxicity={species.toxicity} />
                </Link>
              );
            })}
          </div>
        </section>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npm test -- src/components/zukan/MushroomDetail.test.tsx
```

Expected: All tests pass.

- [ ] **Step 5: Create detail page with generateStaticParams**

`src/app/zukan/[id]/page.tsx`:
```typescript
'use client';

import { useParams } from 'next/navigation';
import { getMushroomById } from '@/data/mushrooms';
import { MushroomDetail } from '@/components/zukan/MushroomDetail';
import { PageHeader } from '@/components/layout/PageHeader';
import { UI_TEXT } from '@/constants/ui-text';

export default function MushroomDetailPage() {
  const params = useParams();
  const id = params.id as string;
  const mushroom = getMushroomById(id);

  if (!mushroom) {
    return (
      <div>
        <PageHeader title={UI_TEXT.zukan.title} showBack />
        <p className="px-4 py-8 text-center text-forest-400">
          キノコが見つかりません
        </p>
      </div>
    );
  }

  return (
    <div>
      <PageHeader title={mushroom.names.ja} showBack />
      <MushroomDetail mushroom={mushroom} />
    </div>
  );
}
```

注意: Static Export で動的ルートを使用するため、`generateStaticParams` が必要。ただし `'use client'` ページでは直接エクスポートできないため、 `next.config.ts` に `dynamicParams: false` の設定は不要（クライアントサイドで解決するため）。もしビルドエラーが出る場合は、以下のようにサーバーコンポーネントでラップする:

```typescript
// 代替案: src/app/zukan/[id]/page.tsx をサーバーコンポーネントにする場合
import { mushrooms } from '@/data/mushrooms';
import MushroomDetailPage from './MushroomDetailClient';

export function generateStaticParams() {
  return mushrooms.map((m) => ({ id: m.id }));
}

export default function Page({ params }: { params: Promise<{ id: string }> }) {
  return <MushroomDetailPage />;
}
```

- [ ] **Step 6: Verify in browser**

```bash
npm run dev
```

ブラウザで以下を確認:
- http://localhost:3000/zukan でグリッドが表示される
- カードクリックで詳細ページに遷移する
- 詳細ページに毒性バッジ、シーズンバー、注意書き、似ている種が表示される
- 「戻る」ボタンで一覧に戻れる

- [ ] **Step 7: Commit**

```bash
git add src/components/zukan/MushroomDetail.tsx src/components/zukan/MushroomDetail.test.tsx src/app/zukan/
git commit -m "feat: add zukan detail page with mushroom info, toxicity caution, and similar species"
```

---

## Task 9: Season Calendar

**Files:**
- Create: `src/components/zukan/SeasonCalendar.tsx`, `src/components/zukan/SeasonCalendar.test.tsx`
- Modify: `src/app/zukan/page.tsx`（カレンダー表示切替を追加）

- [ ] **Step 1: Write failing tests for SeasonCalendar**

`src/components/zukan/SeasonCalendar.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { SeasonCalendar } from './SeasonCalendar';
import { mushrooms } from '@/data/mushrooms';

describe('SeasonCalendar', () => {
  it('renders all mushroom names', () => {
    render(<SeasonCalendar mushrooms={mushrooms} />);
    expect(screen.getByText('マツタケ')).toBeInTheDocument();
    expect(screen.getByText('シイタケ')).toBeInTheDocument();
  });

  it('renders 12 month headers', () => {
    render(<SeasonCalendar mushrooms={mushrooms} />);
    for (let i = 1; i <= 12; i++) {
      expect(screen.getByText(String(i))).toBeInTheDocument();
    }
  });

  it('renders season bars for each mushroom', () => {
    const { container } = render(<SeasonCalendar mushrooms={mushrooms} />);
    const rows = container.querySelectorAll('tbody tr');
    expect(rows.length).toBe(mushrooms.length);
  });

  it('links mushroom names to detail pages', () => {
    render(<SeasonCalendar mushrooms={mushrooms} />);
    const matsutakeLink = screen.getByRole('link', { name: 'マツタケ' });
    expect(matsutakeLink.getAttribute('href')).toBe('/zukan/matsutake');
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
npm test -- src/components/zukan/SeasonCalendar.test.tsx
```

Expected: FAIL.

- [ ] **Step 3: Implement SeasonCalendar**

`src/components/zukan/SeasonCalendar.tsx`:
```typescript
'use client';

import Link from 'next/link';
import { ToxicityBadge } from './ToxicityBadge';
import type { Mushroom } from '@/types/mushroom';

interface SeasonCalendarProps {
  mushrooms: Mushroom[];
}

function isMonthActive(month: number, startMonth: number, endMonth: number): boolean {
  if (startMonth <= endMonth) {
    return month >= startMonth && month <= endMonth;
  }
  return month >= startMonth || month <= endMonth;
}

export function SeasonCalendar({ mushrooms }: SeasonCalendarProps) {
  const currentMonth = new Date().getMonth() + 1;

  return (
    <div className="overflow-x-auto">
      <table className="w-full min-w-[600px] border-collapse">
        <thead>
          <tr>
            <th className="sticky left-0 z-10 bg-forest-900 px-3 py-2 text-left text-xs font-medium text-forest-400">
              種名
            </th>
            {Array.from({ length: 12 }, (_, i) => (
              <th
                key={i + 1}
                className={`px-1 py-2 text-center text-xs font-medium ${
                  i + 1 === currentMonth ? 'text-forest-300' : 'text-forest-500'
                }`}
              >
                {i + 1}
              </th>
            ))}
          </tr>
        </thead>
        <tbody>
          {mushrooms.map((mushroom) => (
            <tr key={mushroom.id} className="border-t border-forest-800">
              <td className="sticky left-0 z-10 bg-forest-900 px-3 py-1.5">
                <div className="flex items-center gap-2">
                  <Link
                    href={`/zukan/${mushroom.id}`}
                    className="text-xs font-medium text-forest-100 hover:text-forest-300 transition-colors"
                  >
                    {mushroom.names.ja}
                  </Link>
                  <ToxicityBadge toxicity={mushroom.toxicity} />
                </div>
              </td>
              {Array.from({ length: 12 }, (_, i) => {
                const month = i + 1;
                const active = isMonthActive(
                  month,
                  mushroom.season.start_month,
                  mushroom.season.end_month
                );
                const isCurrent = month === currentMonth;
                return (
                  <td key={month} className="px-0.5 py-1.5">
                    <div
                      className={`h-4 rounded-sm ${
                        active
                          ? isCurrent
                            ? 'bg-forest-300'
                            : 'bg-forest-500'
                          : 'bg-forest-800/50'
                      }`}
                    />
                  </td>
                );
              })}
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npm test -- src/components/zukan/SeasonCalendar.test.tsx
```

Expected: All tests pass.

- [ ] **Step 5: Add calendar toggle to Zukan page**

`src/app/zukan/page.tsx` を更新:
```typescript
'use client';

import { useState, useMemo } from 'react';
import { mushrooms as allMushrooms, searchMushrooms } from '@/data/mushrooms';
import { MushroomCard } from '@/components/zukan/MushroomCard';
import { SearchFilter } from '@/components/zukan/SearchFilter';
import { SeasonCalendar } from '@/components/zukan/SeasonCalendar';
import { PageHeader } from '@/components/layout/PageHeader';
import { UI_TEXT } from '@/constants/ui-text';
import type { FilterOptions } from '@/types/mushroom';

type ViewMode = 'grid' | 'calendar';

export default function ZukanPage() {
  const [filters, setFilters] = useState<FilterOptions>({});
  const [viewMode, setViewMode] = useState<ViewMode>('grid');

  const filteredMushrooms = useMemo(
    () => searchMushrooms(filters),
    [filters]
  );

  return (
    <div>
      <PageHeader title={UI_TEXT.zukan.title} />

      {/* View toggle */}
      <div className="flex gap-2 px-4 pt-3">
        <button
          onClick={() => setViewMode('grid')}
          className={`rounded-lg px-3 py-1.5 text-sm font-medium transition-colors ${
            viewMode === 'grid'
              ? 'bg-forest-500 text-white'
              : 'bg-forest-800 text-forest-400'
          }`}
        >
          一覧
        </button>
        <button
          onClick={() => setViewMode('calendar')}
          className={`rounded-lg px-3 py-1.5 text-sm font-medium transition-colors ${
            viewMode === 'calendar'
              ? 'bg-forest-500 text-white'
              : 'bg-forest-800 text-forest-400'
          }`}
        >
          {UI_TEXT.zukan.seasonCalendarTitle}
        </button>
      </div>

      {viewMode === 'grid' ? (
        <>
          <SearchFilter filters={filters} onFilterChange={setFilters} />
          {filteredMushrooms.length === 0 ? (
            <p className="px-4 py-8 text-center text-forest-400">
              {UI_TEXT.zukan.noResults}
            </p>
          ) : (
            <div className="grid grid-cols-2 gap-3 px-4 pb-4">
              {filteredMushrooms.map((mushroom) => (
                <MushroomCard key={mushroom.id} mushroom={mushroom} />
              ))}
            </div>
          )}
        </>
      ) : (
        <div className="px-4 py-3">
          <SeasonCalendar mushrooms={allMushrooms} />
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 6: Verify in browser**

```bash
npm run dev
```

ブラウザで http://localhost:3000/zukan にアクセスし:
- 「一覧」「シーズンカレンダー」の切替が動作する
- カレンダーで全13種のシーズンバーが表示される
- 現在の月がハイライトされる
- 種名クリックで詳細ページに遷移する

- [ ] **Step 7: Commit**

```bash
git add src/components/zukan/SeasonCalendar.tsx src/components/zukan/SeasonCalendar.test.tsx src/app/zukan/page.tsx
git commit -m "feat: add season calendar view with monthly season bars and current month highlight"
```

---

## Task 10: Stub Pages, Settings & PWA Configuration

**Files:**
- Create: `src/app/identify/page.tsx`, `src/app/plan/page.tsx`, `src/app/records/page.tsx`, `src/app/settings/page.tsx`, `public/manifest.json`, `public/icons/icon-192x192.png`, `public/icons/icon-512x512.png`

- [ ] **Step 1: Create stub pages for future phases**

`src/app/identify/page.tsx`:
```typescript
'use client';

import { PageHeader } from '@/components/layout/PageHeader';
import { UI_TEXT } from '@/constants/ui-text';

export default function IdentifyPage() {
  return (
    <div>
      <PageHeader title={UI_TEXT.identify.title} />
      <div className="flex flex-col items-center justify-center px-4 py-16">
        <span className="mb-4 text-5xl">🔍</span>
        <p className="text-center text-forest-400">{UI_TEXT.identify.comingSoon}</p>
      </div>
    </div>
  );
}
```

`src/app/plan/page.tsx`:
```typescript
'use client';

import { PageHeader } from '@/components/layout/PageHeader';
import { UI_TEXT } from '@/constants/ui-text';

export default function PlanPage() {
  return (
    <div>
      <PageHeader title={UI_TEXT.plan.title} />
      <div className="flex flex-col items-center justify-center px-4 py-16">
        <span className="mb-4 text-5xl">🗺</span>
        <p className="text-center text-forest-400">{UI_TEXT.plan.comingSoon}</p>
      </div>
    </div>
  );
}
```

`src/app/records/page.tsx`:
```typescript
'use client';

import { PageHeader } from '@/components/layout/PageHeader';
import { UI_TEXT } from '@/constants/ui-text';

export default function RecordsPage() {
  return (
    <div>
      <PageHeader title={UI_TEXT.records.title} />
      <div className="flex flex-col items-center justify-center px-4 py-16">
        <span className="mb-4 text-5xl">📝</span>
        <p className="text-center text-forest-400">{UI_TEXT.records.comingSoon}</p>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Create Settings page**

`src/app/settings/page.tsx`:
```typescript
'use client';

import { PageHeader } from '@/components/layout/PageHeader';
import { UI_TEXT } from '@/constants/ui-text';

export default function SettingsPage() {
  return (
    <div>
      <PageHeader title={UI_TEXT.settings.title} />
      <div className="space-y-6 px-4 py-4">
        {/* App info */}
        <section className="rounded-lg border border-forest-700 bg-forest-800 p-4">
          <h2 className="mb-3 text-sm font-bold text-forest-300">
            {UI_TEXT.settings.appInfo}
          </h2>
          <dl className="space-y-2 text-sm">
            <div className="flex justify-between">
              <dt className="text-forest-400">アプリ名</dt>
              <dd className="text-forest-100">{UI_TEXT.settings.appName}</dd>
            </div>
            <div className="flex justify-between">
              <dt className="text-forest-400">バージョン</dt>
              <dd className="text-forest-100">{UI_TEXT.settings.version}</dd>
            </div>
            <div>
              <dt className="mb-1 text-forest-400">概要</dt>
              <dd className="text-forest-200">{UI_TEXT.settings.appDescription}</dd>
            </div>
          </dl>
        </section>

        {/* Licenses */}
        <section className="rounded-lg border border-forest-700 bg-forest-800 p-4">
          <h2 className="mb-2 text-sm font-bold text-forest-300">ライセンス</h2>
          <p className="text-xs text-forest-400">
            図鑑データ: Wikipedia日本語版 (CC BY-SA 4.0)
          </p>
          <p className="text-xs text-forest-400">
            写真: Wikimedia Commons (CC BY / CC BY-SA)
          </p>
        </section>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Create PWA manifest**

`public/manifest.json`:
```json
{
  "name": "MycoNote - キノコ採取・観察ハンドブック",
  "short_name": "MycoNote",
  "description": "キノコの採取・観察をサポートするオフライン対応ハンドブック",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0a1504",
  "theme_color": "#0a1504",
  "orientation": "portrait-primary",
  "icons": [
    {
      "src": "/icons/icon-192x192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-512x512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}
```

- [ ] **Step 4: Generate placeholder PWA icons**

```bash
# 192x192 placeholder icon (dark green square with mushroom emoji)
npx --yes sharp-cli -i public/images/mushrooms/placeholder.svg -o public/icons/icon-192x192.png resize 192 192
npx --yes sharp-cli -i public/images/mushrooms/placeholder.svg -o public/icons/icon-512x512.png resize 512 512
```

SVGから変換できない場合は、以下のNode.jsスクリプトで生成:
```bash
node -e "
const { createCanvas } = require('canvas');
[192, 512].forEach(size => {
  const c = createCanvas(size, size);
  const ctx = c.getContext('2d');
  ctx.fillStyle = '#0a1504';
  ctx.fillRect(0, 0, size, size);
  ctx.fillStyle = '#8bc34a';
  ctx.font = size * 0.5 + 'px serif';
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText('🍄', size/2, size/2);
  require('fs').writeFileSync('public/icons/icon-' + size + 'x' + size + '.png', c.toBuffer('image/png'));
});
"
```

プレースホルダーアイコンが生成できない場合は、空のPNGファイルを配置してPhase 5で差し替え。

- [ ] **Step 5: Build and verify static export**

```bash
npm run build
```

Expected: ビルドが成功し `out/` ディレクトリに静的ファイルが出力される。

- [ ] **Step 6: Verify all pages work**

```bash
npm run dev
```

全タブをクリックして以下を確認:
- 図鑑: 一覧表示、検索、フィルター、カレンダー、詳細ページ
- 識別: 「Phase 3以降で実装予定」表示
- 計画: 「Phase 3以降で実装予定」表示
- 記録: 「Phase 2以降で実装予定」表示
- 設定: アプリ情報、ライセンス表示

- [ ] **Step 7: Commit**

```bash
git add src/app/identify/ src/app/plan/ src/app/records/ src/app/settings/ public/manifest.json public/icons/
git commit -m "feat: add stub pages, settings page, and PWA manifest with icons"
```

---

## Task 11: E2E Tests & Final Verification

**Files:**
- Create: `playwright.config.ts`, `e2e/zukan.spec.ts`

- [ ] **Step 1: Configure Playwright**

`playwright.config.ts`:
```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  webServer: {
    command: 'npm run dev',
    port: 3000,
    reuseExistingServer: !process.env.CI,
  },
  use: {
    baseURL: 'http://localhost:3000',
  },
  projects: [
    {
      name: 'Mobile Chrome',
      use: { ...devices['Pixel 5'] },
    },
  ],
});
```

- [ ] **Step 2: Install Playwright browsers**

```bash
npx playwright install chromium
```

- [ ] **Step 3: Write E2E tests**

`e2e/zukan.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

test.describe('Zukan (図鑑)', () => {
  test('displays mushroom grid on zukan page', async ({ page }) => {
    await page.goto('/zukan');
    await expect(page.getByText('キノコ図鑑')).toBeVisible();
    // Should display 13 mushroom cards
    const cards = page.locator('a[href^="/zukan/"]');
    await expect(cards).toHaveCount(13);
  });

  test('search filters mushrooms by name', async ({ page }) => {
    await page.goto('/zukan');
    await page.fill('input[placeholder*="検索"]', 'マツタケ');
    const cards = page.locator('a[href^="/zukan/"]');
    await expect(cards).toHaveCount(1);
    await expect(page.getByText('マツタケ')).toBeVisible();
  });

  test('toxicity filter works', async ({ page }) => {
    await page.goto('/zukan');
    await page.click('button:text("猛毒")');
    const cards = page.locator('a[href^="/zukan/"]');
    await expect(cards).toHaveCount(3);
  });

  test('navigates to detail page from grid', async ({ page }) => {
    await page.goto('/zukan');
    await page.click('a[href="/zukan/matsutake"]');
    await expect(page.getByText('マツタケ')).toBeVisible();
    await expect(page.getByText('Tricholoma matsutake')).toBeVisible();
    await expect(page.getByText('食用')).toBeVisible();
  });

  test('toxic mushroom detail shows caution box', async ({ page }) => {
    await page.goto('/zukan/tsukiyo-take');
    await expect(page.getByText('毒')).toBeVisible();
    await expect(page.getByText(/注意事項/)).toBeVisible();
    await expect(page.getByText(/シイタケ・ヒラタケとの誤食/)).toBeVisible();
  });

  test('similar species links work', async ({ page }) => {
    await page.goto('/zukan/tsukiyo-take');
    await page.click('a:has-text("シイタケ")');
    await expect(page.getByText('Lentinula edodes')).toBeVisible();
  });

  test('season calendar view shows all mushrooms', async ({ page }) => {
    await page.goto('/zukan');
    await page.click('button:text("シーズンカレンダー")');
    await expect(page.getByText('マツタケ')).toBeVisible();
    await expect(page.getByText('ツキヨタケ')).toBeVisible();
  });

  test('bottom navigation works', async ({ page }) => {
    await page.goto('/zukan');
    await page.click('a:has-text("設定")');
    await expect(page.getByText('アプリ情報')).toBeVisible();

    await page.click('a:has-text("図鑑")');
    await expect(page.getByText('キノコ図鑑')).toBeVisible();
  });

  test('back button on detail page returns to list', async ({ page }) => {
    await page.goto('/zukan');
    await page.click('a[href="/zukan/matsutake"]');
    await expect(page.getByText('Tricholoma matsutake')).toBeVisible();
    await page.click('button[aria-label="戻る"]');
    await expect(page.getByText('キノコ図鑑')).toBeVisible();
  });
});
```

- [ ] **Step 4: Run E2E tests**

```bash
npm run test:e2e
```

Expected: All E2E tests pass.

- [ ] **Step 5: Run all unit/integration tests**

```bash
npm test
```

Expected: All tests pass.

- [ ] **Step 6: Build check**

```bash
npm run build
```

Expected: Build succeeds with no errors.

- [ ] **Step 7: Commit**

```bash
git add playwright.config.ts e2e/
git commit -m "test: add Playwright E2E tests for zukan list, detail, search, filter, and calendar"
```

- [ ] **Step 8: Create progress tracker**

`docs/progress.md`:
```markdown
# MycoNote 開発進捗

## Phase 1: MVP — 完了
- [x] プロジェクトセットアップ (Next.js 15, Tailwind, Vitest, PWA)
- [x] 型定義・定数
- [x] サンプル図鑑データ (13種)
- [x] AppContext (オンライン状態)
- [x] UI基盤コンポーネント (ToxicityBadge, SeasonBar)
- [x] レイアウト (BottomNav, OfflineBanner, PageHeader)
- [x] 図鑑一覧ページ (検索・フィルター)
- [x] 図鑑詳細ページ (毒性警告・類似種)
- [x] シーズンカレンダー
- [x] スタブページ・設定ページ
- [x] PWA設定 (manifest, icons)
- [x] E2Eテスト

## Phase 2: 記録機能 — 未着手
- [ ] IndexedDB (Dexie.js) セットアップ
- [ ] RecordsContext
- [ ] 記録登録フォーム (GPS, 写真, メモ)
- [ ] 記録一覧ページ
- [ ] 地図表示 (React-Leaflet)
- [ ] 図鑑連携 (詳細ページに自分の記録表示)

## Phase 3: AI連携 — 未着手
- [ ] Claude APIクライアント
- [ ] 詳細識別 (Claude Vision)
- [ ] 採取計画チャット (ハイブリッド方式)
- [ ] APIキー設定UI

## Phase 4: 識別強化 — 未着手
- [ ] TensorFlow.js オフライン識別
- [ ] 簡易識別UI (ダークグリーンテーマ)

## Phase 5: 仕上げ — 未着手
- [ ] エクスポート/インポート
- [ ] パフォーマンス最適化
- [ ] 図鑑データ拡充 (100種)
```

```bash
git add docs/progress.md
git commit -m "docs: add development progress tracker"
```

---

## Phase 1 完了条件チェックリスト

| 条件 | 対応するTask |
|---|---|
| Next.js PWAとしてインストール可能 | Task 1, 10 |
| mushrooms.json に最低100種収録 | Task 3 (13種で開発、後に拡充) |
| 図鑑一覧・詳細・検索・フィルターが動作 | Task 7, 8 |
| シーズンカレンダーが表示される | Task 9 |
| オフラインで基本機能が動作する | Task 1 (PWA config), Task 10 |

注: 100種の収録はPhase 5で対応。Phase 1ではサンプル13種で機能を完成させる。
